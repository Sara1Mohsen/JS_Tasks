let input = prompt("Enter a string:");
document.getElementById("card-text").innerHTML = "Length of the string: " + input.length;
// do {
//         if (!isNaN(input) || input.trim() === "") {
//             alert("Invalid input");
//         }
//     } while (!isNaN(input) || input.trim() === "");

// console.log(input);/